<div id="eintrag_form">

<%= form_for @buying, :as => :buying, :url => buyings_path do |f| %>
 <p>
	<%= f.label :costs %> 
	<%= f.text_field :costs %> €
  </p>
  <p>
	<%= f.label :description %>
	<%= f.text_area :description %>
  </p>
  <p>
	<%= f.submit 'Submit' %>
  </p>
<% end %>

</div>	