<h1>WGs</h1>

<%= link_to 'Sign out', destroy_user_session_path, :method => :delete %>


<%= link_to "Neue WG", new_wg_path %>

<%= current_user.wg_id %>

<ul>
  <% @wgs.each do |wg| %>
    <li><%= link_to wg.name, wg %> | <%= wg.date %> 
  <% end %>
</ul>